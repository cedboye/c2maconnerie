{
  "name": "c2-maconnerie",
  "version": "1.0.0",
  "description": "Site web C2 Maçonnerie",
  "scripts": {
    "dev": "npx live-server --port=3000 --open=/index.html",
    "start": "npx live-server --port=3000 --open=/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
